# 记录

线性结构和非线性结构

线性结构和非线性结构是数据结构中两种基本的组织形式。线性结构是指数据元素之间存在一对一的关系，即每个元素只有一个直接前驱和一个直接后继。常见的线性结构包括数组、链表、栈和队列等。线性结构具有顺序访问的特点，可以通过遍历操作按照顺序逐个访问元素。非线性结构是指数据元素之间存在一对多或多对多的关系，即每个元素可以有多个直接前驱和直接后继，各个元素之间不存在固定的顺序关系。常见的非线性结构包括树和图等。非线性结构具有分支、连接和复杂关联等特点。
