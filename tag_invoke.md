#

`tag_invoke` 是 C++ 中一种新的定制点机制，它允许库的使用者通过定义一个`tag_invoke()`的重载函数来定制库中的行为。这种机制利用了参数依赖查找（ADL）来查找并调用用户定义的重载函数，从而实现对库中特定操作的定制。`tag_invoke`通常与定制点对象（Customization Point Object, CPO）一起使用，CPO 是一个可以被提升为对象的函数，它允许通过 ADL 来调用用户自定义的函数实现。

`tag_invoke`的动机是为了解决 CPO 过多时对命名空间造成大量污染的问题。通过`tag_invoke`，全局只需要预留一个名字`tag_invoke`，而不是为每个 CPO 预留一个名字。这样，用户只需要在自己的命名空间中实现`tag_invoke`函数，而不需要为每个 CPO 都预留一个名字。

在`tag_invoke`的使用中，不同的库定制点对应的`tag_invoke`自定义实现通过`tag_invoke`函数参数中的一个标签类型进行区分。这种方式使得用户只需要预留一个专用的`tag_invoke`标识符，不会对用户命名空间造成大面积污染。

具体来说，`tag_invoke`的使用方式如下：

1. 定义一个 CPO，例如`example_cpo`。
2. 在 CPO 中提供一个或多个模板函数，这些函数可以被友元函数调用，以实现特定的定制行为。
3. 使用`tag_invoke`函数，传入 CPO 和需要定制的参数，调用相应的友元函数实现。

`tag_invoke`是 C++ 标准提案 P1895R0 的一部分，有望随着 sender/receiver 提案一同合并进入 C++26 标准。通过`tag_invoke`，C++ 提供了一种更灵活和更简洁的方式来实现库的定制和扩展。
