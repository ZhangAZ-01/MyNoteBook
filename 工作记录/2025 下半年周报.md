# 2025 下半年周报

2025.07.18

1. 影子精排、新客 MID 用增、FeatureStore 联调。
2. C 位 MergerLog 单独上报粗排全量 item，已上线。
3. 发现 C 位部分 dispatcher（配置扶持的）粗排截断未生效，已修复。
4. Tendis 多版本改为支持配置 identifier+subkey 形式，已合并。
5. wasm 完成 merger C++ 接口实现。
6. 搜索新增透传 qp 字段，已合并，下午（7.18）上线。
7. 修复打分率上报异常（NaN）问题。

2025.07.24

1. 测试 Tendis 多版本软硬回滚符合预期，文档：<https://docs.corp.vipshop.com/docs/q7O0JNmwL0jZHo7L>
2. 搜索透明化链路推全协调，相关性链路未传 abt_id，上游改造中。
3. 排查搜索域新增报错，爬虫用户请求导致，已设置黑名单。
4. 尝试实现搜索统计特征本地模式。
5. merger 联调及客服答疑。

2025.08.01

1. 图搜新增 L0 字段透传，已完成下周发布。
2. 打包最新搜索相关性 jni，自测通过，已完成。
3. 协助解析 rec.feeds、rec.product MergerLog，C 位落地页不采样全量落粗排 item，Kafka 落盘限制调整到 3M。
4. 移除下线 merger2 和 foundation，修改 merger_build 编译脚本。
5. 配置 FeatureStore 监控，链接：<https://m3.vip.vip.com/v3/dashboard/panel/view/1949/8442?refresh=2m&orgId=1&from=now-2h&to=now>
6. 搜索统计特征使用 wasm 插件开发，已基本完成，待测试。
7. 排查线上调用 hub 包过大问题。

2025.08.08

1. merger3.ug.coupon_rank item 分数不符合预期，merger 侧移除 item 排序和截断与上游对齐，已修复。
2. 相关性 doc 向量缺失排查，RocksDB 超时导致。
3. merger3.push_rt 字段缺失排查，上游未传相关字段导致。
4. FeatureStore 联调，排查 L0 Entity key 不一致，搜推对比 L0 Entity value 中。
5. merger 缩小容器模板 merger3.search: 20c48g->16c32g，merger3.rec: 20c40g->16c32g。
6. 搜索统计特征本地模式实现，本地测试通过，待联调。

2025.08.15

1. FeatureStore 联调测试回调接口已完成，今天（8.15）小流量放量观察。
2. merger.search.recall 接口透传图搜和实时曝光商品相关字段，merger.ug.coupon_rank 新增透传字段，已上线。
3. merger.ad.ad 接口支持自定义类型 ContextEntity，已完成周一上线。
4. merger 清理无用的结构定义，优化日志上报内容。
5. merger.rec.list 接口打分率异常排查，模型返回打分过小导致。

2025.08.22

1. merger 支持 Redis 集群灰度，本地测试完成。
 1.1 废除 basic.redis_domain_suffix 配置，改为 redis_items 配置集群标签。
 1.2 支持多 subkey 分单个 subkey 请求 Redis，分 subkey 上报 Redis 监控。
 1.3 支持灰度 Redis 集群，Unific 通过 Appcfg 灰度集群。
2. 推荐新增上下文商品信息到 ContextEntity，搜索新增时间戳字段到 rt 和 listwise 的 ContextEntity，已上线。
3. 搜索粗排配置 did_list，大流量接口耗时下降 8ms，小流量下降 1ms 左右。
4. FeatureStore 支持按请求参数拆分 L0/L1 填充。
5. FeatureStore 耗时分析讨论。

2025.08.29

Merger

1. 排查 C 位扶持数量异常问题，发现按需扶持（cat3_promote）参数配置错误导致影响 Explore 和 EE 扶持不生效，下周上线修复代码（使用实验参数控制新旧扶持逻辑）。
2. 搜索支持透传全人群价格字段，已上线。
3. 搜索相关性支持配置粗排写 DX key，格式为 {rele_query_vec_version}{processed_query}
4. 配合 Hub 验证单独压缩 L0 前后一致性，已完成。
5. 对接 ug 调用推荐 C 位模型。

FeatureStore

1. 改造按请求参数拆分 L0/L1 填充逻辑，新增埋点计时，已上线。
2. 支持调用一次仅返回粗精排路由信息，搜推改造中。
3. 讲解对齐构造 L0/L1 逻辑，ContextEntity 协议同步。

2025.09.05

FeatureStore

1. 简化监控面板以及细化日志上报。
2. 讲解对齐构造 L0 逻辑，搜推预计今天（9.5）联调自行构造 L0/L1。

Merger

1. otd 新增 Context 字段，已上线。
2. merger 部分代码优化，文档：<https://docs.corp.vipshop.com/docs/kgz48nDOrY2bs866>
3. 与算法同学验证修复后扶持逻辑，符合预期。
4. ug 接入 product 接口参数对接，算法放量实验中。

2025.09.12

1. FeatureStore 适配搜索统计特征需求，已上线。
2. merger.search 支持搜索统计特征，支持并行优化后 P99 6ms。
3. merger.search 上线灰度切换 Redis 集群功能，已上线，新增其他场景配置，下周陆续灰度。

2025.09.19

1. 合并 merger 中的业务线程池（optimus、mergerlog、kafka、stastic 使用），仅保留主线程池、Redis 线程池、业务线程池。
2. merger3.search 支持搜索 User 数据二挑一，开发完成待数据准备好后测试上线。
3. 统计特征尝试并行优化，多线程并行等待时间较长效果不明显，暂未上线。
4. merger 支持配置 Redis.broker_id 所有域已推全，旧配置已删除。
5. Review merger3.front 新增 wxk_recall 接口 key 类型。
6. Listwise 新增枚举字段透传，仅改动协议，已发布。
7. merger3.search 10386 灰度到新集群比例 20% 观察中，目前无问题。
8. 和搜推对齐统计特征需求。

问题排查

1. merger3.front.wxk_search 通过 hub 监控发现缺少 ContextEntity，上游 request_id 传 0 导致，已恢复。
2. merger3.search deadline 变少、qps 减少，怀疑是上游切流或监控异常导致，排查未发现问题，八小时自己恢复，不影响效果。

2025.09.26

1. 搜索二挑一开关调整为实验参数，已上线实验。
2. 监控异常排查
  2.1 监控数据丢失排查，suggest 接口上报 abtid 过多导致，已设置将 suggest 的 ClickHouse 上报关闭，待可减少 abtid 上报或支持更大数据量上报时打开。
  2.2 发现相关性和 suggest 目前上报逗号拼接多个 abtid，面板未正确显示，需要搜推调整透传格式。
  2.3 验证搜索域其他接口、OTD 透明化链路上报数据是否正常。
3. 整理搜索域目前异常率较高的 dispatcher，调整 search.recall 接口走非压缩接口耗时下降 7ms。
4. Redis 10386 灰度到新集群比例 50% 观察中，目前无问题。
5. Listwise、搜索场景新增透传字段，已上线。
6. Review merger3.front 新增 Entity 类型代码，已合并。

2025.10.1

联调

1. 创意排序联调，支持 ContextEntity 构造，推理报错算法修复中。
2. 近线召回回调接口联调，支持跳跃 rank flow，测试通过待放量。
3. 搜索透明化监控现支持将缓存从 70K 调整为 300K，suggest 接口监控已打开，目前未发现数据缺失。
4. 流量分配链路升级联调，支持实验参数防呆检测，避免因上游传入负数导致 coredump。

需求 & 优化

1. 统计特征代码优化，已上线，P99 下降 1ms，优化文档：。
2. 相关性新增 QP 字段透传，已上线，算法验收中。
3. FeatureStore 尝试共享配置，搜推本月目标迁移配置，目前 Store 配置保持不变。
4. rec 扩容 16c32g->20c32g，限流时间有下降。
5. 开启推荐 landc 粗排 L0 压缩，减少压缩次数，降低 CPU 使用率。

2025.10.24

1. 对接用增场景将目前调用 product 的流量迁移到 ug 中，预计下周开发完上线。
2. merger3.ug 和 merger3.yrec 支持降级。
3. 将使用 tesseract-log.api.vip.com 场景配置多 broker 落盘。
4. 对接重排和 SearchRT 使用新协议透传。
5. product 支持跳跃 rank flow，已上线。
6. 排查推荐迁移 did 样本缺失问题，配置错误导致。
7. 发现推荐粗排约 40k 大流量走 default，原因包括 dp 配置错误以及古早实验未使用等，记录文档：<https://docs.corp.vipshop.com/docs/AP4ZndO2oXd7hmOm>
8. 参与双十一大促值班。

FeatureStore

1. 对齐讲解 QPEntity 构造，目前所有 Entity 在 api 侧构造。
2. 对接搜索 user_picking 需求。
