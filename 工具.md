# 工具

## LLDB & GDB

### 运行

LLDB & GDB -> `run` or `r` + arguments

### 查看 coredump

gdb 二进制文件 coredump 文件

```bash
source /opt/rh/devtoolset-7/enable
gdb /apps/dat/web/working/server_rec core-server_rec-8475-1730366146
```

bt 查看堆栈
p 打印变量
f 查看具体栈

## 火焰图

[火焰图介绍](https://www.ruanyifeng.com/blog/2017/09/flame-graph.html)

### perf

返回 CPU 正在执行的函数名和调用栈。

通常，它的执行频率是 99Hz（每秒 99 次），如果 99 次都返回同一个函数名，那就说明 CPU 这一秒钟都在执行同一个函数，可能存在性能问题。

```bash
sudo perf record -F 99 -p 13204 -g -- sleep 30
```

上面的代码中，perf record 表示记录，-F 99 表示每秒 99 次，-p 13204 是进程号，即对哪个进程进行分析，-g 表示记录调用栈，sleep 30 则是持续 30 秒。

y 轴表示调用栈，每一层都是一个函数。调用栈越深，火焰就越高，顶部就是正在执行的函数，下方都是它的父函数。

x 轴表示抽样数，如果一个函数在 x 轴占据的宽度越宽，就表示它被抽到的次数多，即执行的时间长。注意，x 轴不代表时间，而是所有的调用栈合并后，按字母顺序排列的。

火焰图就是看顶层的哪个函数占据的宽度最大。只要有"平顶"（plateaus），就表示该函数可能存在性能问题。

## 代码覆盖率

代码覆盖率工具将使用一个或多个标准来确定在执行测试套件期间您的代码是如何执行的。覆盖率报告中的常见指标包括：

函数覆盖率：已定义的函数中有多少被调用。
语句覆盖率：程序中有多少语句已执行。
分支覆盖率：控制结构的分支（例如 if 语句）中有多少已执行。
条件覆盖率：已经测试了多少布尔子表达式的真值和假值。
行覆盖率：已经测试了多少行源代码。

## Jemalloc

Jemalloc 是一种内存分配器，用于优化内存分配和释放的性能。它使用了一种称为 "arenas" 的机制，将内存分配和释放的操作分离开来，从而减少了内存碎片和分配的延迟。

### memory profile

发现并定位内存泄漏。

`MALLOC_CONF=prof_leak:true,lg_prof_sample:19,prof:true,prof_prefix:jeprof.out,prof_final:true,lg_prof_interval:29`

| 参数 | 说明 | 对文件生成的影响 |
|------|------|------------------|
| `prof_leak:true` | 启用内存泄漏检测 | 不直接影响文件数量 |
| `lg_prof_sample:19` | 采样率（每 2^19 次分配采样一次） | 影响精度，不影响文件数量 |
| `prof:true` | 启用性能分析 | 必需参数 |
| `prof_prefix:路径` | 输出文件前缀 | 决定文件位置 |
| `prof_final:true` | 程序退出时生成分析文件 | 生成一个最终文件 |
| `lg_prof_interval:N` | 分析间隔（每 2^N 次分配） | 直接控制文件生成频率 |
| `prof_gdump:true` | 启用信号触发 dump | 允许手动控制 |

/usr/bin/jeprof --show_bytes --svg ./server_rec  --base jeprof.out.xxx.heap jeprof.out.xxx.heap > leak_rec.svg
