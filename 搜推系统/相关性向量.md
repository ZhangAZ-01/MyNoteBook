# 相关性向量

## ItemVec

* CPU 模型（GPDT）负责预估相关性分数，特征设计：
  * Level0
    * QueryVecEntity，查缓存或者实时计算得到 Query 向量；
    * QueryProcessEntity，上游 QP 工程透传
      * 新增 query_rel_term_info 字段
  * Level1
    * ItemVecEntity：商品向量数据
      * KV 类型，key 为商品 id，value 为向量
    * ReleDocEntity：相关性商品数据
      * KV 类型，key 为商品 id，value 包含类目 id、品牌 id、doc length 等；

## QueryDoc

GPU 模型推理，L0 特征为搜索词相关向量

## QueryVec

## ReleDoc

## DSSM Vector

DSSM 模型的全称是 Deep Structured Semantic Model，由微软研究院开发，利用深度神经网络把文本（句子，Query，实体等）表示成向量，应用于文本相似度匹配场景下的一个算法。
其核心思想是将 query 和 doc 映射到到共同维度的语义空间中，通过最大化 query 和 doc 语义向量之间的余弦相似度，从而训练得到隐含语义模型，达到检索的目的。
