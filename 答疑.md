#

## 监控问题

1. 监控中调用 hub 的数据，是每调用一次 hub 就上报一次数据。
2. 调用 hub 的 dispatcher 拼了两个 dispatcher，说明是使用了主副模型，第一个为主模型 dispatcher，第二个为副模型 dispatcher。
3. 接口耗时，是搜推调用 merger 接口的整体耗时。
4. 调用 hub 耗时，是 merger 调用 hub 的整体耗时。

## 配置问题

1. 知道 dispatcher、Entity 后，可以根据 Entity 类型，在 Unified 平台上查找类型，如 SEARCH_QUERY，用这个类型去代码中看 std::string SessionBase::entity_key(int entity_key_type) 使用的是哪个 string_view，再去对应接口中看是取的那个字段。
2. 看使用的 Entity 配置，一定要先确定使用的是 entity_type 还是 did，再看具体 Entity 配置。
3. 如何根据 sorting_id 判断粗精排的日志落盘在哪个表？
4. redis 中的 01 02 03 对应的是集群
5. 如何查看 RocksDB 中有没有某条数据，用 ai_platform/test/tools:rocksdb_tools 工具。
6. merger 支持并发调用 hub，hub 一个请求支持处理多套并发算法。
多模型的另一个用来做特征回传，merger 做融合汇总服务，融合服务用到特征回传的数据。
7. 监控中分 dispatcher rank 耗时和调用 hub 耗时
调用 hub 耗时和算法视图中分模块耗时的 rank 阶段耗时不同是因为调用 hub 耗时只统计状态是 success 的，算法视图中统计的是所有的。
8. 如果 call hub 失败，那就直接返回原样请求，也不走创意以及没有分数和图片 id 这些；如果 call creative 失败，返回里也是没有创意的图片 id 这些信息；如果走了创意，一个商品多张图，就拿分数最高的那张。
9. 双活，配置 gd16 + gd18 集群配置。
10. 通过 redis 前缀在 pdp 查 redis 表名。
11. 新增 entity 要确认需不需要 did 和落样本。
12. 超时和特征缺失填充的默认分数，超时填 0，特征缺失填 -1。
13. 调用 mixer 时，各个打分子项必须在 merger3 的 syntax 中有所体现，不然处理 mixer 时查不到对应的 cxr。
14. 打分监控上报
15. 推理和落样本的 did 区别？怎么配
16. 达标扶持后要截断后再排序，被扶持商品保证在未截断部分但不保证在 topn。
17. 解压 Redis value，使用的是大端解压。(待验证)
18. 按照 in 参数截断时，根据 appcfg 和 merger3_in 配置的哪个参数小用哪个，便于降级。
19. 原路落和旁路落的区别是什么，为什么 rt 只能旁路落？
20. sub_dispatcher 只有 syntax 是必须的，融合到主 dispatcher 使用，和主 dispatcher 接口类型保持一致。

资源信息 + did list，did 配置是为了落样本 先加线上配置再加 did

## 代码实现

1. 融合打分不会回填到 prerank_score/rank_score，但是根据开关选择用不用 mixer_score 排序。
