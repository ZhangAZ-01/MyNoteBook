# 脚本编写

## nohup

no hang up（不挂起），用于在系统后台不挂断地运行命令，退出终端不会影响程序的运行。

## set

```bash
set -e # 当命令执行失败时，立即退出脚本。
set -x # 执行时显示参数和命令。
set +x # 禁止调试。
set -v # 当命令读取时显示输入。
set +v # 禁止打印输入。
```

## 软连接和硬连接

软连接：存放的是源文件路径，指向源文件。

```bash
ln -sf 源文件 目标文件
```

- `-s` ：软连接
- `-f` ：强制执行，链结时先将与 dist 同档名的档案删除。

硬连接：以文件副本的形式存在。

```bash
ln source target
```
